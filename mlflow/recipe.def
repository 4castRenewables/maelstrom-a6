Bootstrap: docker-daemon
From: lifetimes-mlflow:latest

%environment
    export PATH=/venv/bin:$PATH
    # Silence git warning by MLflow
    export GIT_PYTHON_REFRESH=quiet

%files
    data/ /opt/data
    mlflow/train.py /opt/train.py

%runscript
    exec "$@"

%startscript
    exec "$@"
